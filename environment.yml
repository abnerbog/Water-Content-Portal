name: water-content-portal-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - mkdocs
      - mkdocs-material
      - mkdocs-git-revision-date-localized-plugin
      - mkdocs-jupyter
      - mkquartodocs

